import React from 'react';
import { Box, Typography } from '@mui/material';

const Projects = () => {
  return (
    <Box sx={{ padding: '20px' }}>
      <Typography variant="h4">Projetos</Typography>
      <Typography variant="body1">Conteúdo sobre os projetos.</Typography>
    </Box>
  );
};

export default Projects;
